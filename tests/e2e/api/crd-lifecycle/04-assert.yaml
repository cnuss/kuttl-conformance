apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 30
collectors:
  - type: events
    namespace: $NAMESPACE
commands:
  - script: |
      set -e
      if kubectl get crd testwidgets.conformance.kuttl.dev 2>/dev/null; then
        echo "CRD still exists"
        exit 1
      fi
