# yaml-language-server: $schema=https://raw.githubusercontent.com/kyverno/chainsaw/main/.schemas/json/test-chainsaw-v1alpha1.json
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: volume-types
spec:
  description: Validates emptyDir, configMap, secret, downwardAPI, and projected volume types and their read-only enforcement
  steps:
  - name: create-and-verify-volume-types
    try:
    - apply:
        file: 00-install.yaml
    - assert:
        file: 00-assert.yaml
    catch:
    - events: {}
