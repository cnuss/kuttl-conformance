# yaml-language-server: $schema=https://raw.githubusercontent.com/kyverno/chainsaw/main/.schemas/json/test-chainsaw-v1alpha1.json
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: crd-lifecycle
spec:
  description: Validates CRD creation, custom resource CRUD operations, and CRD cleanup
  steps:
  - name: create-crd
    try:
    - apply:
        file: 00-install.yaml
    - assert:
        timeout: 30s
        resource:
          apiVersion: apiextensions.k8s.io/v1
          kind: CustomResourceDefinition
          metadata:
            name: testwidgets.conformance.chainsaw.dev
          status:
            acceptedNames:
              kind: TestWidget
    catch:
    - events: {}
    finally:
    - script:
        content: kubectl delete crd testwidgets.conformance.chainsaw.dev --ignore-not-found
  - name: create-custom-resource
    try:
    - apply:
        file: 01-create-cr.yaml
    - assert:
        file: 01-assert.yaml
    catch:
    - events: {}
  - name: update-custom-resource
    try:
    - apply:
        file: 02-update-cr.yaml
    - assert:
        file: 02-assert.yaml
    catch:
    - events: {}
  - name: delete-and-cleanup
    try:
    - delete:
        ref:
          apiVersion: conformance.chainsaw.dev/v1
          kind: TestWidget
          name: my-widget
    - script:
        content: kubectl wait --for=delete testwidgets.conformance.chainsaw.dev/my-widget -n $NAMESPACE --timeout=30s
    - script:
        content: kubectl delete crd testwidgets.conformance.chainsaw.dev --ignore-not-found
    cleanup:
    - script:
        content: kubectl delete crd testwidgets.conformance.chainsaw.dev --ignore-not-found
